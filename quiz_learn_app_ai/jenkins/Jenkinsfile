pipeline {
    agent {
        docker {
            image 'cirrusci/flutter:stable'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'flutter doctor'
                sh 'flutter pub get'
                sh 'flutter build apk'
            }
        }
    }
}
