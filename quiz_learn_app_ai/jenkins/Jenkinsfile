pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
       

    stage('Setup Android') {
            steps {
                script {
                    def androidSdkRoot = "$WORKSPACE/android-sdk"
                    sh '''
                        rm -rf "$androidSdkRoot"
                        mkdir -p "$androidSdkRoot"
                        cd "$androidSdkRoot"
                        curl -o commandlinetools.zip https://dl.google.com/android/repository/commandlinetools-linux-7302050_latest.zip
                        unzip -q commandlinetools.zip
                        yes | "$androidSdkRoot"/tools/bin/sdkmanager --sdk_root="$androidSdkRoot" "platforms;android-30" "build-tools;30.0.3"
                    '''
                }
            }
        }

         stage('Setup Flutter') {
            steps {
                sh '''
                    rm -rf flutter  # Remove existing Flutter directory if it exists
                    git clone https://github.com/flutter/flutter.git -b stable
                    export PATH="$PATH:`pwd`/flutter/bin"
                    flutter precache
                    flutter doctor
                '''
            }
        }

        stage('Build') {
            steps {
                sh '''
                    export PATH="$PATH:`pwd`/flutter/bin"
                    flutter pub get
                    flutter build apk
                '''
            }
        }
    }
}
